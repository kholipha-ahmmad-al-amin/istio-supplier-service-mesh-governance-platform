import{expect,test}from'vitest';import{routes,MeshError}from'../domain.mjs';const bad=(c,f)=>{try{f();return false}catch(e){return e instanceof MeshError&&e.code===c}};test('governs supplier traffic policies',()=>{const d=routes();d.propose('mesh-engineer','e',{id:'IST-321',supplier:'Northstar Supply',destination:'supplier-risk-api'});expect(bad('conflict',()=>d.route('mesh-operator','o','IST-321','CHG-1'))).toBe(true);d.review('security-governor','g','IST-321','Security review validates supplier service mesh routing controls.');expect(d.route('mesh-operator','o','IST-321','CHG-321').state).toBe('routed');expect(bad('validation',()=>d.propose('mesh-engineer','e',{id:'x',supplier:'x',destination:'x'}))).toBe(true);expect(bad('forbidden',()=>d.review('mesh-engineer','e','IST-321','Security review validates supplier service mesh routing controls.'))).toBe(true);expect(bad('not_found',()=>d.review('security-governor','g','IST-X','Security review validates supplier service mesh routing controls.'))).toBe(true)});
